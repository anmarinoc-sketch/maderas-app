org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
org.gradle.parallel=true
org.gradle.caching=true

android.useAndroidX=true
android.nonTransitiveRClass=true

kotlin.code.style=official

# URL publica del backend. Es el MISMO servidor que XiloScan: el plan gratuito de Render
# solo da 750 horas de instancia al mes y un servicio despierto las consume casi todas,
# asi que un segundo servicio se dormiria y cada consulta tardaria un minuto en despertar.
# Vacia = el APK apunta a 10.0.2.2:3000 (solo emulador).
baseUrl=https://madera-backend.onrender.com
