{
  "name": "madera-backend",
  "version": "0.1.0",
  "private": true,
  "description": "Backend ligero que identifica especies de madera a partir de fotografias del corte transversal usando Gemini.",
  "type": "module",
  "main": "src/server.js",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch --env-file=.env src/server.js"
  },
  "dependencies": {
    "@google/genai": "^1.10.0",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "multer": "^2.0.2"
  }
}
