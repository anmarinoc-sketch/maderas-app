org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
org.gradle.parallel=true
org.gradle.caching=true

android.useAndroidX=true
android.nonTransitiveRClass=true

kotlin.code.style=official

# URL publica del backend, en un unico sitio para todo el proyecto: la usan tanto la
# compilacion del APK como el workflow que mantiene el servidor despierto.
# Vacia = el APK apunta a 10.0.2.2:3000 (solo emulador).
baseUrl=
