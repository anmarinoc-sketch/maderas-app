<?xml version="1.0" encoding="utf-8"?>
<!--
    Solo en compilaciones de debug: permite HTTP en claro para poder apuntar a un backend
    local (10.0.2.2:3000) o a un tunel de pruebas. La compilacion release no lo incluye,
    asi que en produccion la app exige HTTPS.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:usesCleartextTraffic="true"
        tools:ignore="UnusedAttribute" />

</manifest>
